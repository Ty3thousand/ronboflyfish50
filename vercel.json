{
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/node",
        "config": {
          "buildCommand": "npm config set legacy-peer-deps true && npm install && npm run build"
        }
      }
    ]
  }
  